## [0.8.0] — 2026-09-16

### Added

- **lane**: `just preflight` runs every sync --check mode in one local command; the toolchain is pinned to Rust 1.98 via `rust-toolchain.toml`; user-facing PRs without a changie fragment now fail the changelog lane with the rule cited.
- **evidence**: EBCDIC worker-determinism test across all supported codepages (#573); codepage support rows now link exact matrix test functions; #946 commercial-relicensing audit receipt committed under `docs/reports/`.

### Changed

- **surfaces**: #553 dispositions recorded for 0.8.0 — audit stays experimental opt-in, governance stays beta, Arrow stays experimental opt-in, Kafka stays experimental examples; concrete graduation criteria are now in the stability registry, the audit/governance boundary decision is documented, and `architecture check` rejects any `copybook-core` dependency on audit.
