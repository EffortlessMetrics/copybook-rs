# PR Rehydration Queries

## Open PRs
- `is:pr is:open repo:EffortlessMetrics/copybook-rs sort:updated-desc`

## Recent Merges
- `is:pr is:merged repo:EffortlessMetrics/copybook-rs sort:updated-desc`

## Core/Codec Touches
- `is:pr repo:EffortlessMetrics/copybook-rs path:crates/copybook-core/ sort:updated-desc`
- `is:pr repo:EffortlessMetrics/copybook-rs path:crates/copybook-codec/ sort:updated-desc`

## Error Taxonomy
- `is:pr repo:EffortlessMetrics/copybook-rs path:crates/copybook-error/ sort:updated-desc`
