# copybook-json-access

Panic-free JSON object lookup helpers for dot-delimited field paths.

This microcrate centralizes path traversal logic used by higher-level codec encode
validation so behavior stays consistent across API and streaming surfaces.
