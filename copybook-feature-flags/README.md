# copybook-feature-flags

Standalone microcrate for feature-flag contracts and runtime controls used across the copybook-rs governance stack.

## Provides

- `Feature`, `FeatureCategory`, and `FeatureLifecycle`
- `FeatureFlags`, `FeatureFlagsBuilder`, `FeatureFlagsHandle`
- `all_features()` and environment-driven flag loading
