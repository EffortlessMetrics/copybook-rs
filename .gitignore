# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/


# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Claude Code Working Agents
.claude/agents

# Temporary development files
*.log
debug_*
test_actual_*
*_baseline.log
*_results.log
*.lcov
lcov.info

# Backup files
*.bak
*.tmp
*.orig
*.old

# Test data files (use test-data/ directory instead)
test_*.cpy
test_*.jsonl
test_*.bin

# Large generated datasets (shared scripts/gen_dataset.sh & soak workloads)
data/

# Benchmark artifacts
scripts/bench/perf.json

# Stray temp directories (from shell escaping issues)
\$*

# PR/Issue work artifacts (keep these in docs/ or remove after merge)
*-ledger.md
PR_*.md
pr_*.md
*_VALIDATION*.md
*_SUMMARY*.md
*_ASSESSMENT*.md
*_REPORT*.md
*_STATUS*.md
*.jsonl

# Build receipts (keep infrastructure, ignore receipt files)
.runs/*.json
!.runs/schema/

# API baseline temporary files (generated by cargo-semver-checks)
.api-baseline/

# Compiled test/debug binaries (never commit)
test_memory_api
debug_schema
test_zoned_encoding_integration
build_result.txt
clippy_err.txt
test_output.txt
test_counts.txt
